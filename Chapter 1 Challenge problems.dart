/// Challenge Problem 2
/**
 * Make iit compile:
 * age = 16;
 * print(age);
 * age = 30;
 * print(age);
 *
 */
main() {
  // type annotation with int
  // or type inference with var declaration;
  int age = 16;
  print(age);
  age = 30;
  print(age);
}