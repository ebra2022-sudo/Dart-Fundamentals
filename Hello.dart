/// variable in dart

void main() {
  /// Difference between Mutable and immutable object
  /// Mutable objects: these are type of object that
  ///   -> change their internal state
  ///   -> sed for date state is variable for some data processing
  /// Immutable objects: these are type of objects that
  ///   -> doesn't change its internal state after thy are being initialized
  ///   -> used for data safety, consistency and thread safe data manipulation


}